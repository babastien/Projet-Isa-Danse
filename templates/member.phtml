<?php if(!empty($packs)) { ?>

    <?php foreach($packs as $pack) { ?>    
        <div class="pack">
            <a href="<?=constructUrl('/pack', ['pack' => $pack['id']])?>"><img class="pack-image" src="<?=asset($pack['image'])?>" alt="<?= $pack['title'] ?>"></a>
        </div>
    <?php } ?>

<?php } else { ?>
    <p>Vous n'avez souscris à aucun pack pour le moment</p>
<?php } ?>